/** DualBenefit slice barrel - keeps `@/components/sections/narrative/DualBenefit`
 * resolving to the folder so importers (registry seed, renderer) are unchanged. */
export { DualBenefit } from "./DualBenefit";
export type { DualBenefitSection } from "./DualBenefit.types";
