/** SizeSelector slice barrel - keeps `@/components/sections/product/SizeSelector`
 * resolving to the folder so importers (registry seed, renderer) are unchanged. */
export { SizeSelector } from "./SizeSelector";
export type { SizeSelectorSection } from "./SizeSelector.types";
