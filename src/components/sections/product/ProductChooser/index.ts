/** ProductChooser slice barrel - keeps `@/components/sections/product/ProductChooser`
 * resolving to the folder so importers (registry seed, renderer) are unchanged. */
export { ProductChooser } from "./ProductChooser";
export type { ProductChooserSection } from "./ProductChooser.types";
